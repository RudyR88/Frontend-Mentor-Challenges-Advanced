import React from 'react';
import './NotFound.css';

export default function NotFound({theme}) {
    return (
        <main className={`not-found ${theme}-clr`}>
            <h1>item not NotFound</h1>
        </main>
    )
}
